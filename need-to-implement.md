# Need-to-Implement

> Здесь будут фичи которые нужно реализовать и тех. долг

## Технический долг

1. Привести `make spark-run` в рабочее и **best practices**-состояние: перенести всю конфигурацию из `spark-submit` запуска стрима непосредственно в контейнер [`core-site.xml`, `hive-site.xml`] 
    - **State**: `#не_реализовано`