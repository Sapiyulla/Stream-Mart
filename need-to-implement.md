# Need-to-Implement

> Здесь будут фичи которые нужно реализовать и тех. долг

## Технический долг

1. Привести `make spark-run` в рабочее и **best practices**-состояние: перенести всю конфигурацию из `spark-submit` запуска стрима непосредственно в контейнер [`core-site.xml`, `hive-site.xml`, `spark-defaults.conf`].
    - **State**: `#реализовано`

2. Создать инициализационный скрипт `hive_init.sql` и запускать его после старта контейнера `hive-metastore` с помощью ~~**Spark Job**'ы `hive_init.py`~~ `hive_init.sh`. Для этого рекомендуется использовать образ-сборку (из **Dockerfile** в папке `spark/`) как инициализационный контейнер.
    - **State**: `#реализовано`

3. Создать папку-окружение `dwh/` в корне проекта, и все инициализационные скрипты для служебной **Postgres (DWH)** переместить туда.
    - **State**: `#реализовано`